{
  "name": "earl-atnips-place",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run astro dev",
    "build": "bun run astro build",
    "preview": "bun run astro preview",
    "deploy": "bun run build && git add --all && git commit -m \"Site build and deploy\" && git push"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.13",
    "astro": "^5.16.6"
  }
}
